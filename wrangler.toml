# Cloudflare Pages – static site from public/
# Deploy: connect this repo in Cloudflare Dashboard → Pages → Create project → Git,
# or run: npx wrangler pages deploy public --project-name=ai-safety-resources
name = "ai-safety-resources"
compatibility_date = "2024-01-01"
pages_build_output_dir = "public"
